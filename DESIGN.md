# Design — Komari Glass Kv

这是一个面向 VPS 管理者和访客的运维监控主题。首页的唯一任务是让用户在一眼内判断节点状态，并能继续进入详情、Ping、图表和管理工具。

## 视觉世界

- 模式：Operate
- 材质：低噪点毛玻璃、半透明控制条、轻边界和柔和层次
- 色彩：浅色以冷雾蓝为底，暗色以深蓝灰为底；CPU、内存、Swap、硬盘、流量和网络质量使用稳定的多彩语义色
- 结构：工作台式节点网格；每张服务器卡按“系统资料 → 使用 → 流量 → 三网质量”阅读
- 动效：节点加载、数据刷新、质量短柱变化和悬浮反馈；遵守减弱动效设置

## 信息与配置原则

- 数据请求、缓存、RPC、权限、收藏、搜索、Ping/负载历史、费用、导出、拓扑、详情和管理入口属于功能层，保持不变。
- 首页展示层的区块、字段、顺序、卡片密度、快捷工具、总览、背景、价格隐私和详情指标都必须由托管主题设置控制。
- Komari 本体没有的展示选项放入 `komari-theme.json` 的 managed configuration；不修改服务器核心数据模型。
- `custom` 配置使用换行、逗号或空格分隔的 key 列表，非法 key 自动忽略并回退到安全默认值。

## 节点卡结构

1. 标题：国家旗帜、节点名、在线状态、收藏和详情入口。
2. 系统资料：服务商与费用；核心、架构、硬盘、流量、带宽；CPU 型号与系统；运行和剩余时间。前台不显示内核版本。
3. 使用：CPU、内存、Swap、硬盘和流量进度。
4. 网络：实时上传/下载、累计上传/下载。
5. 质量：电信、联通、移动、综合延迟、综合丢包和离散历史短柱。

## 响应式

- 宽屏自动填充多列节点卡。
- 中屏保持双列或紧凑多列，长字段截断并保留悬浮信息。
- 手机单列，区块不横向溢出；点击卡片进入详情，点击质量区打开 Ping 监测。

## 明确不做

- 不删除后台、终端、管理页或已有功能组件。
- 不在前端硬编码服务器价格、地区、服务商或网络质量。
- 不新增 UI 组件库、请求库或重复的数据请求链路。
